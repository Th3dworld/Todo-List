//These functions will be used to set the Title and note values
const setTitle = (title) => title;
const setDescription = (description) => description;
const setDueDate = (dueDate) => dueDate;
const priority = (priority) =>


